const { URL } = require('url');
const PORTAL='Adresowo';
const MAX_PAGES=50;
const FETCH_TIMEOUT_MS=15000;
function num(v){if(v==null)return null;const s=String(v).replace(/\u00a0/g,' ').replace(/\s/g,'').replace(/,/g,'.').replace(/[^0-9.]/g,'');const n=Number(s);return Number.isFinite(n)?n:null;}
function abs(h,b){try{return new URL(h,b).href}catch{return '';}}
function areaFilter(a,target,tol){const t=Number(target);if(!Number.isFinite(t)||t<=0)return a;const p=Number(tol);const lo=t*(1-(Number.isFinite(p)?p:10)/100),hi=t*(1+(Number.isFinite(p)?p:10)/100);return a.filter(x=>x.area>=lo&&x.area<=hi);}
function clean(s){return String(s||'').replace(/<[^>]+>/g,' ').replace(/&nbsp;/gi,' ').replace(/\s+/g,' ').trim();}
function parse(html,base){const offers=[],seen=new Set();const main=html;const re=/<a\b[^>]*href=["']([^"']*\/o\/[^"']+)["'][^>]*>([\s\S]*?)<\/a>/gi;let m;while((m=re.exec(main))){const url=abs(m[1],base);if(!url||seen.has(url))continue;const start=Math.max(0,m.index-1800),end=Math.min(main.length,m.index+2500),window=main.slice(start,end);const price=(window.match(/([0-9][0-9\s\u00a0.]*)\s*zł/i)||[])[1];const ar=(window.match(/([0-9]+(?:[,.][0-9]+)?)\s*m²/i)||[])[1];if(!price||!ar)continue;const p=num(price),a=num(ar);if(!p||!a)continue;const title=clean(m[2]);const loc=(title.match(/Olsztyn[^<]*?(?:ul\.|,|\s)([^<]{0,80})/i)||[])[1]||'';seen.add(url);offers.push({source:PORTAL,type:'Dom',location:title,locality:'Olsztyn',street:loc.trim(),url,price:p,area:a,priceM2:p/a,title});}return offers;}
async function fetchHtml(url){const ac=new AbortController(),tm=setTimeout(()=>ac.abort(),FETCH_TIMEOUT_MS);try{const r=await fetch(url,{redirect:'follow',signal:ac.signal,headers:{'User-Agent':'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/151 Safari/537.36','Accept':'text/html,application/xhtml+xml,*/*','Accept-Language':'pl-PL,pl;q=0.9'}});return{status:r.status,ok:r.ok,url:r.url,html:await r.text()};}finally{clearTimeout(tm);}}
function slugLocation(location='Olsztyn'){return String(location).trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/ł/g,'l').replace(/ą/g,'a').replace(/ę/g,'e').replace(/ó/g,'o').replace(/ś/g,'s').replace(/ź/g,'z').replace(/ż/g,'z').replace(/ć/g,'c').replace(/ń/g,'n').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');}
function nextPageUrl(current){const u=new URL(current);const m=u.pathname.match(/_l(\d+)\/?$/);const n=m?Number(m[1])+1:2;u.pathname=(m?u.pathname.replace(/_l\d+\/?$/,''):u.pathname.replace(/\/$/,'') )+`_l${n}`;return u.href;}
async function searchAdresowoHouses({location='Olsztyn',areaTarget=150,tolerance=10}={}){const start=new URL(`/domy/${slugLocation(location)}/`,'https://adresowo.pl').href;let url=start,pages=0,all=[],recognized=0;const seenPages=new Set();while(url&&pages<MAX_PAGES&&!seenPages.has(url)){seenPages.add(url);const r=await fetchHtml(url);pages++;if(!r.ok)break;const got=parse(r.html,r.url);recognized+=got.length;all.push(...got);if(got.length===0)break;const next=nextPageUrl(r.url);if(next===url)break;url=next;}const uniq=[...new Map(all.map(o=>[o.url,o])).values()];const filtered=areaFilter(uniq,areaTarget,tolerance);return{portal:PORTAL,propertyType:'Dom',requestedLocation:location,url:start,httpStatus:200,fetched:pages>0,recognized,complete:uniq.length,filtered:filtered.length,areaTarget:Number(areaTarget)||null,tolerance:Number(tolerance)||0,radiusSupported:false,pagesFetched:pages,offers:filtered};}
module.exports={PORTAL,parse,searchAdresowoHouses,search:searchAdresowoHouses};