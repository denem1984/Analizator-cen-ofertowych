const http=require('http');const{URL}=require('url');const{searchNieruchomosciOnline}=require('./live-parser');const{searchMorizon}=require('./live-morizon');const{searchDomiporta}=require('./parser-domiporta-v05');const{resolveLocation}=require('./location-resolver');
const PORT=process.env.PORT||10000;const GRATKA_API='https://analizator-cen-ofertowych-v05-gratka.onrender.com/api/gratka';const ADRESOWO_API='https://analizator-cen-ofertowych-v05-adresowo.onrender.com/api/adresowo';
function send(res,status,data){res.writeHead(status,{'Content-Type':'application/json; charset=utf-8','Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'GET,POST,OPTIONS','Access-Control-Allow-Headers':'Content-Type'});res.end(JSON.stringify(data));}
function normalizeUrl(value){try{const u=new URL(value);u.hash='';u.search='';return u.href.replace(/\/$/,'').toLowerCase()}catch{return String(value||'').trim().toLowerCase()}}
function dedupeOffers(offers){const seenUrl=new Set(),seenData=new Set(),duplicates=[],result=[];for(const offer of offers){const urlKey=normalizeUrl(offer.url),price=Number(offer.price),area=Number(offer.area),dataKey=Number.isFinite(price)&&Number.isFinite(area)?`${price}|${area}`:'';let reason='';if(urlKey&&seenUrl.has(urlKey))reason='ten sam URL';else if(dataKey&&seenData.has(dataKey))reason='ta sama cena + ta sama powierzchnia';if(reason){const kept=result.find(x=>(urlKey&&normalizeUrl(x.url)===urlKey)||(dataKey&&`${Number(x.price)}|${Number(x.area)}`===dataKey));duplicates.push({reason,kept:kept||result[0],duplicate:offer});continue}if(urlKey)seenUrl.add(urlKey);if(dataKey)seenData.add(dataKey);result.push(offer)}return{rows:result,duplicates}}
async function remoteJson(url,label){const response=await fetch(url);if(!response.ok)throw new Error(`${label} API HTTP ${response.status}`);return response.json()}
async function searchGratkaRemote({location,areaTarget=62,tolerance=10,radius=0}){const url=`${GRATKA_API}?location=${encodeURIComponent(location)}&area=${encodeURIComponent(areaTarget)}&tolerance=${encodeURIComponent(tolerance)}&radius=${encodeURIComponent(radius)}`,data=await remoteJson(url,'Gratka');return{portal:'Gratka',httpStatus:data.httpStatus,fetched:data.fetched,htmlLength:data.htmlLength,recognized:data.recognized,complete:data.complete,filtered:data.filtered,offers:Array.isArray(data.offers)?data.offers:[],requestedRadius:data.requestedRadius||0,appliedRadius:data.appliedRadius||0,radiusSupported:Boolean(data.radiusSupported)}}
async function searchAdresowoRemote({location,areaTarget=62,tolerance=10,radius=0}){const url=`${ADRESOWO_API}?location=${encodeURIComponent(location)}&area=${encodeURIComponent(areaTarget)}&tolerance=${encodeURIComponent(tolerance)}&radius=${encodeURIComponent(radius)}`,data=await remoteJson(url,'Adresowo');return{portal:'Adresowo',httpStatus:data.httpStatus,fetched:data.fetched,htmlLength:data.htmlLength,recognized:data.recognized,complete:data.complete,filtered:data.filtered,offers:Array.isArray(data.offers)?data.offers:[],requestedRadius:data.requestedRadius||0,appliedRadius:data.appliedRadius||0,radiusSupported:Boolean(data.radiusSupported)}}
async function run({location='Olsztyn',area=62,tolerance=10,radius=0}={}){const resolved=await resolveLocation(location);const canonicalLocation=resolved.name||resolved.input||location;const wojewodztwo=resolved.wojewodztwo||'';const requestedRadius=Math.max(0,Number(radius)||0);const minArea=area*(1-tolerance/100),maxArea=area*(1+tolerance/100),[no,morizon,domiporta,gratka,adresowo]=await Promise.all([searchNieruchomosciOnline({location:canonicalLocation,radius:requestedRadius}),searchMorizon({location:canonicalLocation}),searchDomiporta({location:canonicalLocation,wojewodztwo,areaTarget:area,tolerance,radius:requestedRadius}),searchGratkaRemote({location:canonicalLocation,areaTarget:area,tolerance,radius:requestedRadius}),searchAdresowoRemote({location:canonicalLocation,areaTarget:area,tolerance,radius:requestedRadius})]);const lives=[no,morizon,domiporta,gratka,adresowo],sources=lives.map(live=>{const complete=live.offers.filter(o=>o.locality&&Number.isFinite(o.price)&&Number.isFinite(o.area)&&o.url),filtered=complete.filter(o=>o.area>=minArea&&o.area<=maxArea);const appliedRadius=Number(live.appliedRadius||0);return{portal:live.portal,httpStatus:live.httpStatus,fetched:live.fetched,htmlLength:live.htmlLength,recognized:live.recognized,complete:complete.length,filtered:filtered.length,uniqueAfterCrossPortal:filtered.length,requestedRadius,appliedRadius,searchScope:appliedRadius>0?`+${appliedRadius} km`:canonicalLocation}}),before=sources.flatMap((s,i)=>lives[i].offers.filter(o=>o.locality&&Number.isFinite(o.price)&&Number.isFinite(o.area)&&o.url&&o.area>=minArea&&o.area<=maxArea)),cross=dedupeOffers(before);return{version:'0.6.0-radius',inputLocation:location,resolvedLocation:resolved,location:canonicalLocation,wojewodztwo,area,tolerance,minArea,maxArea,radius:requestedRadius,sources,beforeCrossDedup:before.length,unique:cross.rows.length,duplicatesRemoved:cross.duplicates.length,duplicates:cross.duplicates,offers:cross.rows};}
const server=http.createServer(async(req,res)=>{if(req.method==='OPTIONS')return send(res,204,{});const url=new URL(req.url,`http://${req.headers.host||'localhost'}`);if((req.method==='GET'||req.method==='HEAD')&&url.pathname==='/health')return send(res,200,{ok:true,service:'5-portale-live-v0.6-radius'});if((req.method==='GET'||req.method==='HEAD')&&url.pathname==='/api/resolve-location'){try{return send(res,200,await resolveLocation(url.searchParams.get('location')||''))}catch(e){return send(res,400,{error:e.message||'Nie udało się rozpoznać lokalizacji.'})}}if((req.method==='GET'||req.method==='POST')&&url.pathname==='/api/live/combined'){try{return send(res,200,await run({location:url.searchParams.get('location')||'Olsztyn',area:Number(url.searchParams.get('area')||62),tolerance:Number(url.searchParams.get('tolerance')||10),radius:Number(url.searchParams.get('radius')||0)}))}catch(e){return send(res,500,{error:e.message||'Błąd serwera'})}}return send(res,404,{error:'Nie znaleziono endpointu.'})});
server.listen(PORT,'0.0.0.0',()=>console.log(`5-portale live v0.6-radius listening on port ${PORT}`));